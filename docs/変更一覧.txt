================================================================
ContentsBuilder 変更一覧
================================================================

----------------------------------------------------------------
【CSS変更】
----------------------------------------------------------------

No. | 対象ボタン / UI要素名
    | セレクタ / 対象
    | 変更内容
    | 目的
----------------------------------------------------------------

1   | 詳細設定パネル内「変形・塗り」フィールド
    | .is-field-disabled
    | 新規追加（opacity:0.35、pointer-events:none、user-select:none）
    | アイコンオブジェクト選択時に変形・塗りフィールドをグレーアウト

2   | パネル間の区切り線
    | .side .panel-sep
    | display: none に変更
    | パネル間の横線を非表示

3   | オーサリングパネル・付箋パーツ操作パネルのボタン一覧
    | .side .panel ul
    | margin-bottom: 12px→8px、gap: 12px→8px
    | パネル間スペース縮小

4   | 各パネル内の全ボタン（ページリンク・Plusファイル等）
    | .side .panel ul li
    | height: 64px→56px、width: calc(50%-6px)→calc(50%-4px)
    | ボタン高さ縮小・上詰め

5   | 「オーサリングパネル」「付箋パーツ操作」「整列」「詳細設定」セクションタイトル
    | .side .panel-title
    | margin-top: 12px→8px、margin-bottom: 8px→5px
    | セクションタイトルの余白縮小

6   | 「オーサリングパネル」セクションタイトル（最初の見出し）
    | .side .panel-title:first-of-type
    | 新規追加（margin-top:0）
    | 最初のタイトルの上マージン除去

7   | 「閲覧モードに切替」ボタン
    | .side .mode-btn
    | margin-bottom: 12px→8px
    | モード切替ボタン下マージン縮小

8   | 閲覧モード時の付箋ボタン
    | .sticky-note
    | transition: opacity 0.2s 削除
    | 付箋表示切替を即時化

9   | 閲覧モード時の付箋ボタン（非表示状態）
    | .sticky-note.state-hidden
    | opacity: 0.15→0
    | 閲覧モードで非表示付箋を完全非表示

10  | ナビバー全体（ヘッダー）
    | .nav
    | overflow: hidden、transition: height 0.2s ease 追加
    | ヘッダー折りたたみアニメーション

11  | ナビバー全体（ヘッダー非表示状態）
    | .nav.is-hidden
    | 新規追加（height:0）
    | ヘッダー非表示状態

12  | ヘッダー再表示ボタン（「▼」アイコン）
    | #navRevealBtn
    | 新規追加（fixed配置・右上角）
    | ヘッダー折りたたみ時の再表示ボタン

13  | 詳細設定パネル内「位置・変形・塗り」フォーム
    | .side-detail-form dl
    | 横並び flex に戻す（調整の過程で変更・復元）
    | ラベルレイアウト調整（最終：横並び維持）

14  | 詳細設定パネル内の種別設定フォーム（「リンク先」「ファイル名」等）
    | #dialogFormSpecific
    | 新規追加（display:block、縦積みレイアウト）
    | 種別固有フィールドのラベルを入力欄上部に配置

15  | 詳細設定パネル内の種別設定ラベル（「リンク先」「ファイル名」「表示タイプ」等）
    | #dialogFormSpecific dt
    | 新規追加（width:auto、margin-top:10px、margin-bottom:4px）
    | 種別設定のラベルを上部に表示

16  | 詳細設定パネル内の種別設定ラベル（最初の項目）
    | #dialogFormSpecific dt:first-child
    | 新規追加（margin-top:0）
    | 最初の項目の上マージン除去

17  | 詳細設定パネル内の種別設定入力欄
    | #dialogFormSpecific dd
    | 新規追加（width:auto）
    | 種別設定のdd幅フル幅化

18  | 詳細設定パネル内の「表示方法」「再生方法」「動画ファイル」ラジオボタンラベル
    | .side-detail-form .radio-row label
    | 新規追加（color:#dde）
    | ラジオラベルを明るい色で表示


----------------------------------------------------------------
【HTML変更】
----------------------------------------------------------------

No. | 対象ボタン / UI要素名
    | 変更内容
    | 目的
----------------------------------------------------------------

19  | 詳細設定パネル内「変形（W・H）」「塗り」フィールドのdt/dd要素
    | data-field-group="transform" / data-field-group="fill" 属性追加
    | グレーアウト対象の識別

20  | 付箋パーツ操作パネル「付箋一括設定」ボタン
    | 削除
    | 不要機能の除去

21  | 付箋パーツ操作パネル「付箋グループ化」ボタン
    | 削除
    | 「付箋グループ切替」ボタンに統合

22  | 付箋パーツ操作パネル「グループ解除」ボタン
    | 削除
    | 「付箋グループ切替」ボタンに統合

23  | 付箋パーツ操作パネル「付箋グループ切替」ボタン（新規）
    | 新規追加（onclick="toggleStickyGroup()" / 「付箋一括設定」の位置）
    | グループ化/解除の統合ボタン

24  | ヘッダー再表示「▼（ヘッダーを表示）」ボタン（新規）
    | 新規追加（</nav> 直後・下矢印SVG・id="navRevealBtn"）
    | ヘッダー折りたたみ時の再表示ボタン

25  | ナビバー右端「ナビを折りたたむ」ボタン（上矢印）
    | onclick="toggleNav()"・data-tip 更新
    | ヘッダー折りたたみトリガー

26  | サイドバーの「整列」パネルと「詳細設定」パネル
    | HTML上の順序を入れ替え（整列→詳細設定 の順に変更）
    | パネル並び順変更


----------------------------------------------------------------
【JavaScript変更】
----------------------------------------------------------------

No. | 対象ボタン / UI要素名
    | 関数名
    | 変更内容
    | 目的
----------------------------------------------------------------

27  | 詳細設定パネル内「変形・塗り」フィールド
    | buildCommonFields
    | グレーアウト対象フィールドに .is-field-disabled を付与する処理追加
    | アイコンオブジェクト選択時の変形・塗りグレーアウト

28  | 詳細設定パネルの「種別設定」セクション
    | openAnnotationSettingsDialog
    | if (existingEl) → if (existingEl && type !== 'sticky') に変更
    | 付箋選択時に種別設定セクションを非表示

29  | ページ上の付箋ボタン（クリック操作）
    | addStickyClickHandler
    | Shift なしクリックで既選択オブジェクトをクリック → 選択維持に変更
    | 複数選択の選択状態を保持

30  | 閲覧モード時の付箋ボタン（クリック操作）
    | addStickyClickHandler
    | clearTimeout + setTimeout(220ms) を削除し即時実行に変更
    | 付箋表示/非表示トグルの即時化

31  | ページ上のアノテーションボタン（クリック操作）
    | addAnnClickHandler
    | Shift なしクリックで既選択をクリック → 選択維持に変更
    | 複数選択の選択状態を保持

32  | ページ上の全ボタン（ドラッグ操作）
    | makeDraggable
    | 複数選択時の一括ドラッグブロック追加
    | 複数選択した付箋・アノテーションを同時移動

33  | ページ上の全ボタン（Alt+ドラッグコピー操作）
    | makeDraggable
    | Alt+ドラッグコピー後、クローンに選択を移動（altCopied フラグ導入）
    | コピー後にクローンが選択状態になる

34  | ページ上の全ボタン（複数選択時のAlt+ドラッグコピー）
    | makeDraggable / onUpMulti
    | コピー後に元オブジェクトの選択解除・クローンを選択リストに追加
    | 複数選択時のAlt+コピー後選択移動

35  | 詳細設定パネル内の付箋「内容」テキストエリア
    | buildSpecificFields
    | sticky ブランチの annText textarea 削除、即 return に変更
    | 付箋の内容入力フィールド廃止

36  | 「付箋一括設定」ダイアログ内の「内容」テキストエリア
    | openBulkStickyDialog
    | bulkAnnText フィールド・生成・保存処理すべて削除
    | 付箋一括設定のテキストフィールド廃止

37  | 付箋パーツ操作「付箋グループ化」ボタン（後方互換）
    | groupStickyNotes
    | 既存関数を維持
    | 後方互換保持

38  | 付箋パーツ操作「グループ解除」ボタン（後方互換）
    | ungroupStickyNotes
    | 既存関数を維持
    | 後方互換保持

39  | 付箋パーツ操作「付箋グループ切替」ボタン
    | toggleStickyGroup（新規）
    | 新規追加（全員グループ済み→解除 / 未グループ有→グループ化、2件以上必要）
    | グループ化/解除の統合ロジック

40  | ナビバー右端「ナビを折りたたむ」ボタン（上矢印）
    | toggleNav
    | 実装追加（.nav.is-hidden トグル・「▼（ヘッダーを表示）」ボタン表示切替・ツールチップ更新）
    | ヘッダー折りたたみ機能


----------------------------------------------------------------
【データ定数変更】
----------------------------------------------------------------

No. | 対象ボタン / UI要素名
    | 定数名
    | 変更内容
    | 目的
----------------------------------------------------------------

41  | 詳細設定パネル内「塗り」カラー選択（アノテーション用）
    | ANN_COLOR_OPTIONS
    | 5色→3色（青・緑・黄）、「（半透明）」ラベル削除
    | 色選択肢の簡略化

42  | 詳細設定パネル内「塗り」カラー選択（付箋用）
    | STICKY_COLORS
    | 7色→3色（青・緑・黄）
    | 付箋色選択肢の簡略化

43  | 付箋の背景色表示
    | STICKY_COLOR_MAP
    | ['#4488cc', '#5ac46e', '#f7e04b'] に更新
    | 付箋色マップを3色に対応


================================================================
【2026-03-05 追加変更点】
================================================================

----------------------------------------------------------------
【CSS変更】
----------------------------------------------------------------

No. | 対象ボタン / UI要素名
    | セレクタ / 対象
    | 変更内容
    | 目的
----------------------------------------------------------------

44  | クイック作成ポップアップ（ライトテーマ背景）
    | .quick-popup .dialog-contents
    | background: #eee に変更
    | サイドバー（ダークテーマ）と区別するためのライトテーマ適用

45  | クイック作成ポップアップ内の入力フィールド
    | .quick-popup .side-detail-form .d-input
    | 新規追加（背景 #fff、枠線 #ccc、文字色 #333）
    | ライトテーマ用入力フィールドカラー

46  | クイック作成ポップアップ内のスピンボタン
    | .quick-popup .spin-btn
    | 新規追加（背景 #fff、枠線 #bbb）
    | ライトテーマ用スピンボタンカラー

47  | 整列パネル全体
    | .align-panel
    | 新規追加（padding: 6px 10px 10px）
    | 整列パネルのレイアウト定義

48  | 整列パネル（非活性状態）の整列ボタン
    | .align-panel.is-disabled .align-btn
    | 新規追加（opacity: 0.35; pointer-events: none）
    | 単一選択・非選択時の整列ボタン非活性表示

49  | 各整列ボタン
    | .align-btn
    | 新規追加（高さ 30px 等）
    | 整列ボタンのスタイル定義

50  | 選択バウンディングボックス
    | #selectionBoundingBox
    | 新規追加（position:absolute; border:2px dashed rgba(104,152,216,0.8); background:transparent; pointer-events:none; z-index:900）
    | 複数選択時の選択範囲ボックス表示

----------------------------------------------------------------
【HTML変更】
----------------------------------------------------------------

No. | 対象ボタン / UI要素名
    | 変更内容
    | 目的
----------------------------------------------------------------

51  | 整列パネル（新規）
    | <div class="align-panel"> ブロックをサイドバーに追加（SVGアイコンのみの整列ボタン×6・ツールチップ付き）
    | 整列機能のUIをサイドバーに追加

----------------------------------------------------------------
【JavaScript変更】
----------------------------------------------------------------

No. | 対象ボタン / UI要素名
    | 関数名
    | 変更内容
    | 目的
----------------------------------------------------------------

52  | クイック作成ポップアップのフォーム統合
    | openQuickCreateDialog()
    | buildCommonFields / buildSpecificFields を流用するよう全面書き直し
    | ポップアップ内でもサイドバーと同等の全パラメータを設定可能に

53  | クイック作成確認処理
    | confirmQuickCreate()
    | qcFormCommon / qcFormSpecific の値を dialogFormCommon / dialogFormSpecific に転送後 confirmAnnotation() を呼ぶよう修正
    | ポップアップの値をサイドバー経由で確定できるよう統合

54  | 表示タイプ ID 競合バグ修正
    | buildSpecificFields() 内
    | annDisplayType の取得を document.getElementById → dtDd.querySelector にスコープ限定
    | 同一ページに複数フォームが存在する場合の ID 競合を回避

55  | ドラッグ即時作成（マーカー形式）
    | onPageMouseUp()
    | 一定距離以上ドラッグした場合はポップアップを開かず annDisplayType='marker' 固定で即時作成
    | 操作ステップを削減し直感的なアノテーション配置を実現

56  | 新規オブジェクトのサイズ優先設定
    | confirmAnnotation()
    | 新規作成時に pendingRect の寸法をフォーム値より優先して使用するよう修正
    | ドラッグで描画したサイズを正確に反映

57  | 選択動作の改善（全選択クリア→対象のみ選択）
    | addStickyClickHandler() / addAnnClickHandler()
    | Shift なしクリック時に付箋・アノテーション全選択をクリアしてからクリック対象のみ選択するよう修正
    | クリック選択の一貫性を向上させ、意図しない複数選択を防止

58  | 選択オブジェクト一覧取得
    | getSelectedObjects()（新規）
    | 選択中の付箋＋アノテーション全オブジェクトを配列で返す関数を新規追加
    | 複数選択処理の共通基盤として複数関数から利用

59  | 整列パネル更新処理
    | updateAlignPanel()（新規）
    | 選択数に応じたパネル活性／非活性切替、バウンディングボックスの描画・更新・削除を行う関数を新規追加
    | 整列UIを選択状態に連動して動的制御

60  | 整列実行処理
    | alignObjects(mode)（新規）
    | getBoundingClientRect 基準で左・水平中央・右・上・垂直中央・下の6方向整列を実行する関数を新規追加
    | 複数オブジェクトを指定方向に整列する機能

61  | ステータス更新フック追加
    | updateStatus()
    | updateAlignPanel() の呼び出しを追加
    | ステータス変化と連動して整列パネルも更新されるよう

62  | ライブアップデートのデルタ方式化
    | applyLiveUpdate()
    | 複数選択時の X/Y/W/H 更新をデルタ（差分）適用に変更（単一選択は絶対値適用のまま）。_prevX/_prevY/_prevW/_prevH 関数プロパティで前回値を状態管理
    | 複数選択時に各オブジェクトの相対位置・個別サイズを維持したまま一括変更可能に

63  | デルタ基準値リセット
    | openAnnotationSettingsDialog() / closeDialog()
    | applyLiveUpdate の _prevX/_prevY/_prevW/_prevH をリセット
    | ダイアログ開閉時にデルタ計算値を初期化し計算ズレを防止

64  | 複数選択 Alt+ドラッグ一括コピー
    | makeDraggable()
    | 2件以上選択中に選択内オブジェクトを Alt+ドラッグすると全選択オブジェクトをクローンしてまとめて移動。reinitElement() で各クローンにクリックハンドラ・リサイズを再設定。Shift+Alt で水平/垂直軸固定にも対応
    | 複数選択オブジェクトを相対位置を維持したまま一括コピー移動できる操作を実現

================================================================
【2026-03-06 追加変更点】
================================================================

----------------------------------------------------------------
【CSS変更】
----------------------------------------------------------------

No. | 対象ボタン / UI要素名
    | セレクタ / 対象
    | 変更内容
    | 目的
----------------------------------------------------------------

65  | パネル間の区切り線（No.2 の変更を上書き）
    | .side .panel-sep
    | display: none → border-top: 1px solid #3a3f4a（仕切り線として表示）
    | パネル間の仕切り線を可視化してセクション境界を明確化

66  | 詳細設定パネルコンテナ
    | #sideDetailPanel
    | display: grid に変更
    | 空状態・活性状態を同一グリッドセルに重ね配置するための基盤

67  | 詳細設定パネル（空状態）
    | #sideDetailEmpty
    | grid-row: 1 / grid-column: 1 を設定し同一セルに配置
    | 活性/非活性切替時にレイアウトがずれないよう固定

68  | 詳細設定パネル（活性状態）
    | #sideDetailActive
    | grid-row: 1 / grid-column: 1 を設定し同一セルに配置
    | 活性/非活性切替時にレイアウトがずれないよう固定

69  | 詳細設定パネル フッター（活性状態）
    | #sideDetailActive .side-detail-footer
    | display: none を追加
    | フッター空高さをゼロにしてレイアウトずれをさらに解消

70  | リサイズハンドル（上辺中点）
    | .resize-handle[data-corner="tc"]
    | 新規追加（top: -5px; left: calc(50% - 4px); cursor: n-resize）
    | 8点ハンドルの上辺中点を追加

71  | リサイズハンドル（下辺中点）
    | .resize-handle[data-corner="bc"]
    | 新規追加（bottom: -5px; left: calc(50% - 4px); cursor: s-resize）
    | 8点ハンドルの下辺中点を追加

72  | リサイズハンドル（左辺中点）
    | .resize-handle[data-corner="ml"]
    | 新規追加（top: calc(50% - 4px); left: -5px; cursor: w-resize）
    | 8点ハンドルの左辺中点を追加

73  | リサイズハンドル（右辺中点）
    | .resize-handle[data-corner="mr"]
    | 新規追加（top: calc(50% - 4px); right: -5px; cursor: e-resize）
    | 8点ハンドルの右辺中点を追加

74  | ステータスバー CSS 削除
    | .status-bar / .status-bar .status-item / .status-bar .dot
    | スタイル定義を削除
    | ステータスバー廃止に伴う CSS の整理

75  | アノテーションオブジェクト（選択時）
    | .ann-object.is-selected
    | overflow: visible を追加
    | 選択ハンドルが overflow:hidden によりオブジェクト内側にクリップされるバグを修正

----------------------------------------------------------------
【HTML変更】
----------------------------------------------------------------

No. | 対象ボタン / UI要素名
    | 変更内容
    | 目的
----------------------------------------------------------------

76  | 詳細設定パネル内のヒントテキスト
    | #sideDetailEmpty 内の「オーサリングパネルのボタンを選択すると入力できます」を削除
    | 不要なヒントテキストを除去し UI を簡略化

77  | ステータスバー
    | <div class="status-bar"> ブロック全体を削除
    | ステータスバーを廃止

----------------------------------------------------------------
【JavaScript変更】
----------------------------------------------------------------

No. | 対象ボタン / UI要素名
    | 関数名
    | 変更内容
    | 目的
----------------------------------------------------------------

78  | 付箋コピー時のリサイズハンドル付与
    | copySelectedSticky()
    | makeDraggable(clone) の直後に makeResizable(clone) を追加
    | コピーして作成した付箋もリサイズ可能にする

79  | 詳細設定ヒントテキスト削除（JS テンプレート）
    | _renderGrayedPanel()
    | テンプレート文字列内の「オブジェクトを選択すると入力できます」を削除
    | 不要なヒントテキストを除去し UI を簡略化

80  | 詳細設定空状態の表示切替方式変更
    | sideDetailEmpty 切替処理
    | style.display による切替 → style.visibility による切替に変更
    | 非活性時も DOM 領域を維持してグリッドレイアウトのずれを防止

81  | リサイズハンドル 8点対応（単体選択）
    | makeResizable(el)
    | ハンドル生成リストを 4点（コーナーのみ）→ 8点に拡張。辺中点（tc/bc）は縦のみ・(ml/mr)は横のみリサイズ。コーナーは Shift+ドラッグで軸固定
    | より細かい方向指定のリサイズを実現

82  | リサイズハンドル 8点対応（複数選択バウンディングボックス）
    | addBoundingBoxHandles(box)
    | 同様に 8点に拡張。辺中点ハンドルでは scaleX または scaleY の片方のみ変化。コーナーハンドルは Shift+ドラッグ軸固定対応（単体選択と同仕様）
    | 複数選択時でも単軸リサイズを可能にする

83  | ステータスバー廃止（ページ番号更新削除）
    | updatePageDisplay()
    | statusPage 要素への DOM 更新行を削除
    | ステータスバー廃止に伴う JS の整理

84  | ステータスバー廃止（倍率更新削除）
    | resizePage()
    | #statusZoom strong への DOM 更新行を削除
    | ステータスバー廃止に伴う JS の整理

85  | ステータスバー廃止（選択状態更新削除）
    | updateStatus()
    | statusSelected 要素への参照・テキスト更新を削除（updateAlignPanel() 呼び出しは維持）
    | ステータスバー廃止に伴う JS の整理

86  | オーサリングパネルボタン切替時の選択解除
    | activateAnnotationMode(type)
    | 別ボタンへの切替分岐に deselectAllObjects() と closeDialog() を追加
    | ボタン切替時に選択状態とダイアログをリセットし操作の一貫性を向上

================================================================
合計変更点：86件（2026-03-06 時点）
  CSS変更    : 36件（18 + 7 + 11）
  HTML変更   : 11件（ 8 + 1 +  2）
  JS変更     : 36件（14 + 13 +  9）
  定数変更   :  3件
================================================================
