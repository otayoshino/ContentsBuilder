# ContentsBuilder 開発ログ

---

## 2026-03-04

### 作業内容

#### 1. ローカルサーバーの起動（別PCからのアクセス）
- Pythonの簡易HTTPサーバーをポート8000で起動
- コマンド: `python -m http.server 8000`
- このPCのIPアドレス: `128.10.200.52`
- 別PCからのアクセスURL: `http://128.10.200.52:8000/mock/index.html`
- 同一ネットワーク（Wi-Fi / 社内LAN）が必要

#### 2. モックの再作成
- 既存の `mock/index.html` を削除
- `sample/sample.html`（既存Webアプリのキャプチャ）を参考に新規作成

---

### 新モック（mock/index.html）の構成

#### ナビゲーションバー
| 要素 | 内容 |
|------|------|
| 背景色 | `#647cc0`（オーサリングモード配色） |
| サイドバーラベル | 「AUTHORING MODE」（背景 `#576ba6`、幅270px） |
| ページ操作 | 目次・最初・前・ページ番号入力（1〜144）・次・最後 |
| 拡大縮小 | 拡大（+10%）・縮小（-10%） |
| フィット | ページ全体・高さ・幅（標準選択） |

#### サイドバー（オーサリングパネル）
- モード切替ボタン（閲覧モードに切替）
- アノテーション追加パネル：
  - ページリンク / Plusファイル / 外部リンク / 音声再生 / 動画再生 / **付箋**
- 付箋パーツ操作パネル：
  - 付箋（コピー）/ 付箋一括設定 / 付箋グループ化

#### ビューエリア
- 見開き2ページ表示（左右各420×560px）
- サンプルアノテーションを3種配置（音声再生・ページリンク・外部リンク）
- クリックで選択状態になる

#### ダイアログ（アノテーション設定）
- アノテーション種別ごとに専用フォームを動的生成
- 保存・削除・キャンセルボタン
- オーバーレイ外クリックで閉じる

#### ステータスバー
- オーサリングモード表示
- 現在のページ番号・表示倍率

---

### 次回セッションの引き継ぎ事項

- Phase 1 作業中（3月中が目標）
- モックは `mock/index.html` に存在。サンプル（`sample/sample.html`）を参考に作成済み
- サーバー起動コマンド: `python -m http.server 8000`（プロジェクトルートで実行）
- 次のタスク候補:
  - [ ] モックの動作確認・細部調整
  - [ ] ボタン名称の変更（開発計画書 Phase 1）
  - [ ] ボタン配色の変更（開発計画書 Phase 1）
  - [ ] ボタンのプロパティ編集画面の修正（開発計画書 Phase 1）
  - [ ] コード整理（重複処理のクラス化）

---

## 2026-03-04（続き）

### 作業内容

#### 4. モック見直し：縦フィット（高さに合わせる）をデフォルト表示に変更
- ページサイズを固定値（420×560px）から動的リサイズに変更
- ビューエリアの高さに合わせてページを拡縮する `resizePage()` 関数を実装
- フィットモード（`height` / `width` / `page`）を状態変数で管理
- ウィンドウリサイズ時にも追従するよう `window.addEventListener('resize', resizePage)` を追加
- フィットボタンのデフォルト選択を「幅に合わせる」→「高さに合わせる」に変更

#### 3. モック見直し：単ページ表示への変更
- 見開き2ページ表示（左右ページ）→ 単ページ表示に変更
- `mock/index.html` のページコンテナから右ページ（`#pageRight`）を削除
- ページ上に配置していたサンプルアノテーションアイコン（音声再生・ページリンク・外部リンク）を削除
- JS側の `pageRight` 参照も合わせて削除

---

## 2026-03-04（続き②）

### 作業内容

#### 5. 付箋：ドラッグで任意サイズのボックスを作成する機能を追加

**概要**
- 「付箋」パネルボタンを押下するとページ上で描画モードに移行
- ページ上でドラッグして任意サイズの矩形を作成
- マウスアップ後に付箋設定ダイアログを表示
- ダイアログ保存後、指定位置・サイズに付箋ノートを配置

**追加CSS（`mock/index.html`）**
| セレクタ | 用途 |
|---|---|
| `.sticky-note` | 配置済み付箋ボックス |
| `.sticky-content` | 付箋テキストエリア |
| `.sticky-corner` | 右下の折り返しコーナー |
| `.sticky-delete-btn` | ホバー時に表示される削除ボタン |
| `.sticky-draw-preview` | ドラッグ中の点線プレビュー矩形 |
| `.page.drawing-mode` | 描画モード時のクロスヘアカーソル |

**追加JS関数**
| 関数名 | 役割 |
|---|---|
| `activateStickyMode()` | 描画モードを有効化 |
| `deactivateStickyMode()` | 描画モードを無効化 |
| `onPageMouseDown(e)` | ページのmousedownイベント（描画開始） |
| `onPageMouseMove(e)` | ページのmousemoveイベント（プレビュー更新） |
| `onPageMouseUp(e)` | ページのmouseupイベント（矩形確定・ダイアログ表示） |
| `openStickySettingsDialog()` | 付箋設定ダイアログを動的生成して表示 |
| `confirmStickyNote()` | ダイアログ保存時に付箋をページへ配置 |
| `makeDraggable(el)` | 付箋を移動可能にする |
| `selectStickyNote(el)` | 付箋クリックで選択状態を切り替える |
| `deleteStickyNote(el)` | 付箋を削除する |

**操作フロー**
1. 「付箋」ボタン押下 → ドラッグモード（ステータスバーにメッセージ表示）
2. ページ上でドラッグ → 点線プレビュー矩形が表示される
3. マウスアップ → 設定ダイアログが開く（内容・背景色・フォント）
4. 「保存」 → 付箋が指定位置に配置（ドラッグで移動可能、ホバーで削除ボタン表示）
5. Escキー → 描画モードをキャンセル

---

> 次回セッション開始時は「開発ログを読み込んで」と伝えると、前回の続きから作業できます。
